use super::argument_lexer::ArgumentToken;

// Tokenize Command Line Arguments
pub fn tokenize(source: String) -> Vec<ArgumentToken> {
    let arg_tokens: Vec<ArgumentToken> = Vec::new();

    return arg_tokens;
}
