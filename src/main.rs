use std::fmt;

mod command {
    mod argument_tokenizer;
    use argument_tokenizer::tokenize_arguments;
}

// Main Entry Point
fn main() -> () {
    println!("Hyaena Technologies");

    succesful_exit();

    return ();
}
